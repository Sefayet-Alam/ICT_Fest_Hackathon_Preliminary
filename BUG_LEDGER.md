# BUG_LEDGER — CoWork Booking API

Source of truth = `README.md` (16 business rules + API contract). Grading is black-box.
Status legend: **CONFIRMED** (verified by reading code vs spec) · **SUSPECTED** (needs runtime repro) · **FIXED** · **VERIFIED**.

All line numbers are pre-fix (against the `Initial commit`).

| ID | Rule | Bug | Evidence (file:line) | Expected | Actual | Repro | Testcase | Fix plan | Status |
|----|------|-----|----------------------|----------|--------|-------|----------|----------|--------|
| B1 | 3 | Overlap check uses `<=`, rejects legal back-to-back bookings | `app/routers/bookings.py:50` (`b.start_time <= end and start <= b.end_time`) | Overlap iff `existing.start < new.end AND new.start < existing.end`; back-to-back allowed | Booking ending exactly when another starts → false `ROOM_CONFLICT` | Book 10–11, then 11–12 same room → 409 | T-CONF-BACK2BACK | Change both comparisons to strict `<` | CONFIRMED |
| B2 | 2 | 300s past-start grace window | `app/routers/bookings.py:86` (`start <= now - timedelta(seconds=300)`) | `start_time` strictly in the future; no grace | Start up to 5 min in the past accepted (201) | Book start = now−60s → 201 | T-WIN-PAST | Reject when `start <= now` | CONFIRMED |
| B3 | 2 | Missing min-duration (≥1h) and `end>start` checks | `app/routers/bookings.py:89-94` (only checks whole-hours and `>8`) | Duration whole hours, 1..8; `end` strictly after `start` | Duration 0 (end==start) or negative accepted → price 0/negative | Book start==end → 201, price 0 | T-WIN-ZERO, T-WIN-NEG | Reject `duration_hours < MIN_DURATION_HOURS (1)` and `end <= start` | CONFIRMED |
| B5a | 11 | List ordering DESC instead of ASC | `app/routers/bookings.py:137` (`start_time.desc()`) | Ascending `start_time`, ties ascending `id` | Descending order | GET /bookings with ≥2 bookings | T-LIST-ORDER | `Booking.start_time.asc()` | CONFIRMED |
| B5b | 11 | Pagination offset off-by-one page | `app/routers/bookings.py:139` (`.offset(page * limit)`) | Page N → items `[(N−1)·L, N·L)` | Page 1 skips first `limit` rows | GET /bookings?page=1 misses newest | T-LIST-PAGE1 | `.offset((page-1) * limit)` | CONFIRMED |
| B5c | 11 | Hardcoded `.limit(10)` ignores `limit` param | `app/routers/bookings.py:139` (`.limit(10)`) | Honor `limit` (1..100) | Always returns ≤10 | GET /bookings?limit=2 returns 10 | T-LIST-LIMIT | `.limit(limit)` | CONFIRMED |
| B6 | 10 | `GET /bookings/{id}` missing member ownership check (IDOR) | `app/routers/bookings.py:156-163` (org filter only) | Member reads only own booking; other member's id → 404 | Any member reads any booking in org | Member A GETs member B's booking id → 200 | T-VIS-IDOR-GET | Add `if user.role != 'admin' and booking.user_id != user.id: 404` | CONFIRMED |
| B7 | contract | `GET /bookings/{id}` overwrites `start_time` with `created_at` | `app/routers/bookings.py:166` | `start_time` = booking start | Returns created_at as start_time | GET a booking, compare start_time | T-DETAIL-STARTTIME | Delete the overwrite line | CONFIRMED |
| B8a | 6 | Refund tier boundary `>48` and integer-hour floor | `app/routers/bookings.py:200-201` | notice ≥48h → 100% | notice exactly 48h (or 48.x) → 50% | Cancel with notice=48h → 50% | T-REFUND-48 | Compare `notice >= timedelta(hours=48)` (timedelta, not floored hours) | CONFIRMED |
| B8b | 6 | `<24h` notice returns 50% instead of 0% | `app/routers/bookings.py:205-206` (else → 50) | notice <24h → 0% | Returns 50% refund | Cancel with notice=1h → 50% | T-REFUND-0 | else → `refund_percent = 0` | CONFIRMED |
| B9 | 6 | Refund rounding wrong + response≠RefundLog | `app/routers/bookings.py:208` (`round()` banker's) & `app/services/refunds.py:17` (`int()` truncation) | Half-cent rounds UP; response == stored | round() bankers + int() truncation; two values can diverge | 50% of 1001 → 500 (want 501); values differ for some inputs | T-REFUND-ROUND, T-REFUND-CONSISTENT | Single source: `amount = (price_cents*percent + 50)//100`; log_refund returns it; router echoes it | CONFIRMED |
| A1 | 8 | Access token lifetime = 54000s not 900s | `app/auth.py:50` (`timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES * 60)`) with config `=15` | `exp − iat` = exactly 900 | 15×60 min = 54000s | Decode access token, check exp−iat | T-JWT-EXP | `timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)` | CONFIRMED |
| A2 | 8 | Logout stores `jti` but checks `sub` → logout never works | `app/auth.py:86` (adds jti) vs `:97` (checks sub) | Logout invalidates presented access token (reuse → 401) | Token still valid after logout | Logout then reuse token → 200 | T-AUTH-LOGOUT | Check `payload.get('jti') in _revoked_tokens` | CONFIRMED |
| A4 | 15 | Duplicate username returns 200 not 409 | `app/routers/auth.py:37-43` | Duplicate username in org → 409 `USERNAME_TAKEN` | Returns 200 with existing user | Register same org+username twice | T-REG-DUP | Raise `AppError(409,'USERNAME_TAKEN',…)` | CONFIRMED |
| A5 | 8 | Refresh token not single-use | `app/routers/auth.py:81-93` (no invalidation) | Refresh rotates + invalidates presented refresh (reuse → 401) | Same refresh token reusable indefinitely | Call /auth/refresh twice with same token → both 200 | T-AUTH-REFRESH-REUSE | Blacklist refresh `jti` on use; reject if already used | CONFIRMED |
| T1 | 1 | Offset datetimes dropped, not converted to UTC | `app/timeutils.py:12-13` (`replace(tzinfo=None)`) | Offset input converted to UTC before storage | `+06:00` kept as wall-clock, wrong instant | Book `...T12:00:00+06:00`, check stored/echoed UTC | T-TZ-OFFSET | `dt.astimezone(timezone.utc).replace(tzinfo=None)` | CONFIRMED |
| RM1 | 13 | Cancel does not invalidate availability cache (stale) | `app/routers/bookings.py:217` (only invalidates report) | Availability reflects current state immediately | Cancelled booking still shown busy | Cache availability, cancel, re-GET → still busy | T-AVAIL-STALE | Add `cache.invalidate_availability(room_id, start.date().isoformat())` on cancel | CONFIRMED |
| AD1 | 12 | Create does not invalidate usage-report cache (stale) | `app/routers/bookings.py:121` (only invalidates availability) | Report reflects current state immediately | New booking missing from cached report | GET report, create booking, GET report → unchanged | T-REPORT-STALE | Add `cache.invalidate_report(user.org_id)` on create | CONFIRMED |
| S1 | 14 | Room stats from in-memory counters, drift/race vs DB | `app/services/stats.py` + `app/routers/rooms.py:110` | Stats always equal values derivable from bookings | In-memory counter races/drifts; wrong after concurrency or restart | Concurrency or restart → stats ≠ DB | T-STATS-DERIVED | Compute live from DB (`count`/`sum` confirmed bookings for room) | CONFIRMED |
| EX1 | 9 | Export `include_all`+`room_id` bypasses org scope (cross-org leak) | `app/services/export.py:22-29,49-50` (`fetch_bookings_raw` no org filter) | Only own-org data on every path; cross-org id → 404 | Admin exports another org's bookings by room_id | Admin calls /admin/export?include_all=true&room_id=<other-org room> | T-EXPORT-CROSSORG | Always scope by org; validate room in org else 404 | CONFIRMED |
| REF1 | 7 | Reference-code counter race → duplicates | `app/services/reference.py:17-21` (read-modify-write + sleep); `app/models.py:55` not `unique` | Reference codes unique even under concurrency | Concurrent creates share a code | Fire N concurrent /bookings, inspect reference_code | T-REF-UNIQUE | Guard counter with `threading.Lock`; add `unique=True` on column | CONFIRMED |
| N1 | 16 | Notification lock-ordering deadlock (create vs cancel) | `app/services/notifications.py:24-35` (email→audit vs audit→email) | Service never hangs under concurrent valid requests | Concurrent create+cancel deadlock → hang | Concurrent create & cancel → request hangs | T-LIVENESS-DEADLOCK | Use one consistent lock order (or a single lock) | CONFIRMED |
| RL1 | 5 | Rate limiter not thread-safe (miscount) | `app/services/ratelimit.py:18-26` (unguarded read-modify-write + sleep) | 20/60s per user holds under concurrency | Concurrent requests lose updates → limit bypassable | Fire >20 concurrent /bookings for one user | T-RATE-CONC | Guard bucket mutation with `threading.Lock` | CONFIRMED |
| B-CONC | 3,4 | Double-booking & quota not atomic under concurrency | `app/routers/bookings.py:42-71,100-103` (check-then-insert, sleeps widen race) | Conflict/quota hold under concurrent requests | Two concurrent bookings for same slot both succeed | Concurrent identical /bookings → 2×201 | T-CONF-CONC, T-QUOTA-CONC | Serialize conflict-check+insert (process lock) or DB-level guard | CONFIRMED |

## Notes
- Deliberate `time.sleep()` hooks exist in `bookings.py`, `ratelimit.py`, `reference.py`, `stats.py`, `notifications.py` — they widen concurrency race windows so the concurrency bugs are observable. The fix is correct locking/atomicity, not merely removing sleeps.
- Fix order (low-risk, high-value first): T1 → B1 → B2 → B3 → B7 → B6 → B5a/b/c → A4 → B8a/b → B9 → A1 → A2 → A5 → EX1 → RM1 → AD1 → S1 → REF1 → RL1 → N1 → B-CONC.
